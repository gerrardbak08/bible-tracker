import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler: true, // disabled: causes memoization issues with useRef pattern in useChurchProgress
};

export default nextConfig;
